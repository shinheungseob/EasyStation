import 'package:flutter/material.dart';

class cameraUi extends StatelessWidget {
  const cameraUi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('camera ui interface');
  }
}
