import 'package:flutter/material.dart';

var theme = ThemeData(
    appBarTheme: AppBarTheme(
      color: Colors.blue,
    ),
    textTheme: TextTheme(
        headlineMedium: TextStyle(
            color: Colors.grey
        )
    )
);